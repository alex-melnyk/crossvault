name: crossvault_platform_interface
description: Platform interface for crossvault - a secure cross-platform vault plugin for storing sensitive data using native secure storage (Keychain, Keystore, Credential Manager).
version: 1.0.0
homepage: https://github.com/alex-melnyk/crossvault
repository: https://github.com/alex-melnyk/crossvault/tree/main/crossvault_platform_interface
issue_tracker: https://github.com/alex-melnyk/crossvault/issues

topics:
  - security
  - encryption
  - platform-interface
  - secure-storage

environment:
  sdk: ^3.9.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
